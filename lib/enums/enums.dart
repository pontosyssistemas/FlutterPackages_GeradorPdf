enum TextDecorationEnum { none, underline, overline, lineThrough }

enum AlignmentEnum { left, center, right }

enum TiposParametros {
  texto,
  simNao,
  numerico,
  monetario,
  data,
  dataInicial7Dias,
  dataInicial30Dias,
  dataFinal7Dias,
  dataFinal30Dias,
  cpf,
  cnpj,
  filial,
  filialLogada,
  simNaoPadraoNao,
  dataPermiteNulo,
}
